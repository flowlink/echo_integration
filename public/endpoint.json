{
    "name": "echo",
    "display": "Echo Test Endpoint",
    "description": "Testing endpoint",
    "help": "http://guides.spreecommerce.com/integration/endpoints/echo",
    "consumers": [
        {
            "name": "received",
            "path": "/",
            "description": "Echo's received message in 'recieved' key of response, with a HTTP 200 response code.",
            "requires": {
                "parameters": []
            }
        },
        {
            "name": "echo",
            "path": "/echo",
            "description": "Echo the exact message received.",
            "requires": {
                "parameters": []
            }
        },
        {
            "name": "fail",
            "path": "/fail",
            "description": "Echo's received message in 'recieved' key of response, with a HTTP 500 response code.",
            "requires": {
                "parameters": []
            }
        }
    ]
}
